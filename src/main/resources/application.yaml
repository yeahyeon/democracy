spring:
    main:
        allow-bean-definition-overriding: true

    r2dbc:
        url: r2dbc:mysql://localhost:3306/vote
        username: root
        password: dPdmsdk?0928

r2dbc:
    pool:
        enabled: true
        max-size: 20

server:
    address: 0.0.0.0
    port: 8080
